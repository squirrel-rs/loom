[package]
name = "loom"
version = "0.1.0"
edition = "2024"

[dependencies]
camino = "1.2.2"
loom_rt = { path = "../loom_rt" }
loom_common = { path = "../loom_common" }
miette = { version = "7.6.0", features = ["fancy"] }
clap = { version = "4.5.59", features = ["derive"] }
